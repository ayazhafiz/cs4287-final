apiVersion: v1
kind: Service
metadata:
  name: js-engine
spec:
  selector:
    app: js-engine
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: js-engine
  labels:
    app: js-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: js-engine
  template:
    metadata:
      labels:
        app: js-engine
    spec:
      containers:
      - name: js-engine
        image: ayazhafiz/runjavascript:latest
        ports:
        - containerPort: 9000
